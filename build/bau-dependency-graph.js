"use strict";
var BauDependencyGraph = (function () {
    /**
     * @param {BauSourceFile[]} sources is a flat array of to-be-nodes,
     * each one exposing its dependencies (which are other ones from the array,
     * OR NOT)
     */
    function BauDependencyGraph(sources) {
    }
    return BauDependencyGraph;
}());
exports.BauDependencyGraph = BauDependencyGraph;
